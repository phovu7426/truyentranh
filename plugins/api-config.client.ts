export default defineNuxtPlugin(() => {
  // Runtime config is automatically available via useRuntimeConfig()
  // No need to manually inject into window.__NUXT__
  // Nuxt handles this automatically
})
